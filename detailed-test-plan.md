# 在线诊疗系统分阶段详细测试计划（V1）

> 制定日期：2026-04-01  
> 适用范围：`medical-back`、`medical-patient-front`、`medical-front-doctor/back-vue-master`  
> 执行原则：**功能点逐一验证 -> 真实数据环境验证 -> 集成联调验证 -> 回归验证**。

---

## 1. 目标与验收口径

### 1.1 总体目标
1. 对项目功能点建立“可执行、可复现、可追踪”的测试资产。
2. 单元测试优先覆盖核心业务规则与边界条件。
3. 接口测试必须连接真实依赖（MySQL/Redis/RabbitMQ/MinIO）并使用真实业务数据。
4. 集成测试覆盖患者端、医生端、管理端三条主链路。
5. 每完成一批测试，立即写入 `test-execution-log.md`（含证据命令、输入、输出、结论）。

### 1.2 通过标准（DoD）
- 单元测试：关键模块通过率 100%，无阻塞级失败。
- 接口测试：核心接口成功/异常分支均有记录，结果与预期一致。
- 集成测试：三端主路径全部走通，状态流转正确。
- 可追溯性：每个功能点至少有一条执行记录与证据。

---

## 2. 测试环境与数据基线

## 2.1 依赖服务
- MySQL（业务数据主库）
- Redis（挂号预占、状态缓存）
- RabbitMQ（异步持久化）
- MinIO（文件上传）
- Spring Boot 后端（`medical-back`）
- 患者端（`medical-patient-front`）
- 医生/管理端（`medical-front-doctor/back-vue-master`）

## 2.2 数据基线脚本
- `scripts/OnlineTreat_medical_only.sql`
- `scripts/20260312_registration_person_key.sql`
- `scripts/20260321_cleanup_demo_data.sql`

## 2.3 固定测试样本（后续执行中持续补充）
- 患者账号：`userId=3`（dev token）
- 医生账号：`userId=5`（dev token）
- 科室/排班：执行时记录具体 ID
- 房间/挂号：执行时记录具体 ID

---

## 3. 功能点拆解与测试矩阵

## 3.1 阶段 B：单元测试（后端）

### B1 AI 模块
- `AiRequestSanitizer`：敏感字段脱敏、空输入、超长输入。
- `AiRateLimiter`：窗口限流、边界阈值、并发下计数一致性。
- `AiRequestContext`：上下文构造、字段缺失容错。

### B2 挂号状态与键规则
- `AppointmentReservationState`：状态迁移合法性、非法迁移阻断。
- `AppointmentReservationKeys`：Redis key 生成稳定性与唯一性。
- `PatientIdentityUtil`：`personKey` 在多身份证件场景的稳定生成。

### B3 时间策略与工具类
- `ScheduleTimePolicy`：排班时间边界（开始=结束、跨天、非法范围）。

### B4 服务层核心逻辑（增补）
- `RegistrationServiceImpl`：重复挂号拦截、库存校验。
- `MedicalRecordServiceImpl`：病历新增/查询关键分支。
- `PrescriptionServiceImpl`：处方创建完整性校验。

---

## 3.2 阶段 C：真实数据接口测试（按功能逐点）

1. 登录与鉴权（devToken、普通登录、无 token 访问）
2. 就诊人管理（增删改查、身份证重复约束）
3. 科室/子科室/排班查询
4. 挂号创建（成功、重复、库存不足、异常回滚）
5. 问诊房间流程（发起、接受、拒绝、超时）
6. 聊天消息（文本、图片、历史消息）
7. 病历（新增、列表、详情）
8. 处方（新增、详情、患者侧可见性）
9. AI 患者分诊 + 医生协作接口
10. 管理端基础 CRUD（医生/患者/药品/排班模板）

---

## 3.3 阶段 D：异步与一致性测试

1. Redis 预占 -> MQ 投递 -> DB 落库一致性
2. 预占超时释放（zset 过期）
3. 重复请求幂等性（同人同排班）
4. 预约状态与房间状态联动一致性
5. 病历状态与处方状态联动一致性

---

## 3.4 阶段 E：前后端集成测试

### 患者端链路
登录 -> 选就诊人 -> 查排班 -> 挂号 -> 预约列表 -> 接受问诊 -> 聊天 -> 查看病历/处方 -> AI 分诊。

### 医生端链路
登录 -> 查看挂号 -> 发起问诊 -> 聊天 -> 填病历 -> 开处方 -> 病历列表 -> AI 协作。

### 管理端链路
登录 -> 科室管理 -> 医生管理 -> 患者管理 -> 药品管理 -> 排班模板维护。

---

## 4. 执行节奏（按批次推进）

- 批次 1：B1 + B2（先完成最关键规则层）
- 批次 2：B3 + B4（补齐服务层单测）
- 批次 3：C1~C4（登录/就诊人/排班/挂号）
- 批次 4：C5~C8（问诊/聊天/病历/处方）
- 批次 5：C9~C10 + D（AI/管理端/异步一致性）
- 批次 6：E（跨端集成）+ 回归

每个批次产出：
1. 测试用例清单（输入、步骤、预期、实际）
2. 命令与接口证据
3. 缺陷与回归项
4. `test-execution-log.md` 更新

---

## 5. 风险与应对

- 外网依赖不可达导致 Maven/npm 下载失败：记录为环境阻塞并切换离线可执行项。
- 测试数据污染：每批次前后执行清理脚本并记录变化。
- 异步链路难复现：增加日志采样点（Redis key、MQ messageId、DB 主键）。

---

## 6. 当前执行状态（2026-04-01）

- [x] 详细测试计划建立
- [ ] 批次 1（B1+B2）执行完成
- [ ] 批次 2（B3+B4）执行完成
- [ ] 批次 3（C1~C4）执行完成
- [ ] 批次 4（C5~C8）执行完成
- [ ] 批次 5（C9~C10+D）执行完成
- [ ] 批次 6（E+回归）执行完成


## 7. 批次 1（B1+B2）详细用例设计（逐功能点）

### 7.1 B1-01 `AiRequestSanitizer` 输入清洗
- 用例 B1-01-1：普通文本不过滤（期望 `sanitized=false` 或内容不变）。
- 用例 B1-01-2：包含手机号/身份证号（期望敏感片段被掩码）。
- 用例 B1-01-3：空字符串/null（期望返回可处理默认值，不抛异常）。
- 用例 B1-01-4：超长输入（期望长度控制策略生效）。

### 7.2 B1-02 `AiRateLimiter` 限流
- 用例 B1-02-1：窗口内首次请求（期望放行）。
- 用例 B1-02-2：窗口内达到阈值（期望最后一个放行/拒绝符合配置）。
- 用例 B1-02-3：超过阈值后继续请求（期望拒绝并给出重试提示）。
- 用例 B1-02-4：窗口过期后请求（期望恢复放行）。

### 7.3 B1-03 `AiRequestContext` 请求上下文
- 用例 B1-03-1：完整字段构造（期望上下文字段齐全）。
- 用例 B1-03-2：缺少可选字段（期望容错并提供默认值）。
- 用例 B1-03-3：非法来源字段（期望标准化/拒绝非法值）。

### 7.4 B2-01 `AppointmentReservationState` 状态机
- 用例 B2-01-1：`PROCESSING -> PAYING` 合法迁移。
- 用例 B2-01-2：`PAYING -> SUCCESS/FAILED` 合法迁移。
- 用例 B2-01-3：跨阶段非法迁移（期望拒绝）。
- 用例 B2-01-4：终态再次迁移（期望保持终态或拒绝）。

### 7.5 B2-02 `AppointmentReservationKeys` Key 规则
- 用例 B2-02-1：同 schedule+person 生成 key 稳定一致。
- 用例 B2-02-2：不同 schedule 或 person 生成 key 不冲突。
- 用例 B2-02-3：边界值（空/0/null）输入处理策略验证。

### 7.6 B2-03 `PatientIdentityUtil` personKey 规则
- 用例 B2-03-1：身份证主键生成稳定。
- 用例 B2-03-2：无身份证时回退字段生成稳定。
- 用例 B2-03-3：同人多次请求 key 不变，不同人 key 不同。

### 7.7 批次 1 执行与记录要求
1. 每个用例记录：输入、步骤、预期、实际、结论。
2. 每个模块至少提供 1 条命令行证据（mvn 测试命令或日志片段）。
3. 若环境阻塞，必须记录阻塞编号、时间、重试命令与恢复条件。
