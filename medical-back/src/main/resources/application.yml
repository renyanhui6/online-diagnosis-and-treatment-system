server:
  servlet:
    context-path: /treat
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://47.122.118.231:3306/OnlineTreat?serverTimezone=Asia/Shanghai
    username: root
    password: Qwer_1234
  data:
    redis:
      timeout: 20000
      host: 47.122.118.231
      port: 6379
      password: Qwer_1234
  mail:
    host: smtp.qq.com
    port: 587
    username: 2679327648@qq.com  # 发件邮箱
    password: vebbhptoptrdebeb  # QQ邮箱授权码（非登录密码）
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true  # TLS加密
          ssl:
            trust: smtp.qq.com
  jackson:
    time-zone: Asia/Shanghai
    date-format: yyyy-MM-dd HH:mm:ss
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

  rabbitmq:
    host: 47.122.118.231
    port: 5672
    username: admin
    password: Qwer_1234
    virtual-host:
    connection-timeout: 10000ms
    address-shuffle-mode:


upload:
  # MinIO 配置（本地对象存储）
  endpoint: ${MINIO_ENDPOINT:}
  access-key: ${MINIO_ACCESS_KEY:}
  secret-key: ${MINIO_SECRET_KEY:}
  bucket: ${MINIO_BUCKET:}
  # 可选：对外访问域名前缀（例如 http://localhost:9000/bucket 或反向代理域名）
  public-url-prefix: ${MINIO_PUBLIC_URL_PREFIX:}



mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

netty:
  ws:
    enabled: true
    port: 9001
    path: /netty/ws/chat
    idle-seconds: 90

ai:
  deepseek:
    base-url: ${DEEPSEEK_BASE_URL:https://api.deepseek.com}
    api-key: ${DEEPSEEK_API_KEY:}
    model: ${DEEPSEEK_MODEL:deepseek-chat}
    timeout-seconds: ${DEEPSEEK_TIMEOUT_SECONDS:20}




#vebbhptoptrdebeb
