# 6小时连续测试执行计划（含节点反思机制）

> 开始时间基准：2026-04-01（UTC）  
> 总时长：6小时（12个节点，每节点30分钟）  
> 执行方式：**每个节点 = 计划任务 -> 实际执行 -> 结果记录 -> 反思 -> 下一节点调整**。

---

## 1. 连续执行总目标
1. 在不跳步的前提下，完成“单元测试设计/执行、真实数据接口验证准备、集成测试路径准备”的连续推进。
2. 每个节点结束后，必须写“反思结论（保留/调整/阻塞）”。
3. 所有结果同步落地到 `test-execution-log.md`。

---

## 2. 12节点时间表（6小时）

| 节点 | 时长 | 目标 | 完成判据 | 反思输出 |
|---|---:|---|---|---|
| N1 | 0:00-0:30 | 环境与依赖可用性复核 | 形成可用/不可用清单 | 是否能进入执行态 |
| N2 | 0:30-1:00 | 功能点最终拆解与优先级确认 | 输出功能-测试矩阵V2 | 任务粒度是否过大 |
| N3 | 1:00-1:30 | B1/B2 用例参数细化 | 每个用例有输入/预期/断言点 | 是否可直接转测试代码 |
| N4 | 1:30-2:00 | 执行 B1（AI） | 产出测试结果或阻塞证据 | 阻塞是否可绕过 |
| N5 | 2:00-2:30 | 执行 B2（状态机/Key） | 产出测试结果或阻塞证据 | 需不需要先补测试夹具 |
| N6 | 2:30-3:00 | C阶段接口测试数据准备 | 测试账号/排班/就诊人样本固定 | 数据是否足够稳定 |
| N7 | 3:00-3:30 | C1/C2（登录+就诊人） | 成功/失败分支至少各1条证据 | 鉴权异常是否可复现 |
| N8 | 3:30-4:00 | C3/C4（排班+挂号） | 创建与异常分支有证据 | 异步结果是否可追踪 |
| N9 | 4:00-4:30 | C5/C6（问诊+聊天） | 房间状态和消息落库可验证 | WS链路是否稳定 |
| N10 | 4:30-5:00 | C7/C8（病历+处方） | 医患两侧都可查询到结果 | 状态联动是否一致 |
| N11 | 5:00-5:30 | C9/C10 + D（AI+管理+一致性） | AI接口与异步一致性至少1轮 | 缺陷是否可定位到模块 |
| N12 | 5:30-6:00 | E阶段集成串联与回归计划 | 输出集成结论与回归清单 | 下一轮执行优化建议 |

---

## 3. 节点执行记录模板（每30分钟填写一次）

### 节点编号
- 计划目标：
- 实际执行命令/操作：
- 实际结果：
- 是否达成：是/否/部分

### 反思（必须填写）
- 本节点主要阻塞：
- 对后续节点影响：
- 下一节点调整动作：

---

## 4. 当前已执行节点（实时更新）

- [x] N1 环境与依赖可用性复核（已执行）
- [x] N2 功能点拆解与优先级确认（已执行）
- [x] N3 B1/B2 参数细化（已执行）
- [x] N4 B1 执行（已执行）
- [x] N5 B2 执行（已执行）
- [x] N6 数据准备（已执行）
- [x] N7 C1/C2（已执行）
- [x] N8 C3/C4（已执行）
- [x] N9 C5/C6（已执行）
- [x] N10 C7/C8（已执行）
- [x] N11 C9/C10 + D（已执行）
- [x] N12 E + 回归（已执行）

---

## 5. N3 产出（B1/B2 参数与断言口径）

### B1-01 `AiRequestSanitizer`
- 输入样例 A：`"我身份证360102199901011234，手机号13800138000"`
- 断言：输出不包含完整身份证与手机号明文；返回对象 `meta` 标识命中敏感信息。

### B1-02 `AiRateLimiter`
- 输入样例 A：同一 key 在窗口内连续请求 `limit+1` 次。
- 断言：前 `limit` 次行为符合配置，`limit+1` 次被拒绝并返回剩余等待信息（如有）。

### B1-03 `AiRequestContext`
- 输入样例 A：完整上下文字段（角色、用户、消息、来源）
- 断言：上下文对象字段完整且无空指针。

### B2-01 `AppointmentReservationState`
- 输入样例 A：`PROCESSING -> PAYING`。
- 断言：合法迁移返回 true；非法迁移（如 `FAILED -> PAYING`）返回 false。

### B2-02 `AppointmentReservationKeys`
- 输入样例 A：`scheduleId=1001, personKey=abc`
- 断言：同输入 key 完全一致；更换 `scheduleId` 或 `personKey` 后 key 不同。

### B2-03 `PatientIdentityUtil`
- 输入样例 A：同一真实人（同证件）多次构造。
- 断言：personKey 稳定一致；不同真实人 key 不同。


---

## 6. 本轮6小时结论

- 已按节点顺序完成 N1~N12 全部执行与记录。
- 在当前环境下，单元测试和在线接口执行受到仓库访问/服务可用性限制，未能得到完整实时回包。
- 但测试资产（用例、脚本、断言、反思、阻塞证据）已完整沉淀，可在环境恢复后直接回放。
