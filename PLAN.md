# 项目计划（六个工期）

## 总体目标
- 以“挂号—问诊—病历/处方”为核心链路，完成在线诊疗系统的可运行闭环。
- 技术统一：后端以 Spring Boot + 原生 WebSocket 为唯一长连接实现；前端两端共用统一状态与接口规范。
- 范围收敛：仅保留核心业务与必要管理功能，移除占位/无后端支撑的接口与页面逻辑。

## 统一约束与范围收敛
- WebSocket：统一使用 `ws://host:8080/treat/ws/chat/{roomId}`。
- 挂号状态：0 待支付（保留状态，不走支付流程）、1 已支付、2 排队中、3 问诊中、4 已完成、5 暂时挂起、6 已回归、7 等待患者确认、8 失效。
- 管理端统计与医生端“占位统计/报表类接口”暂不实现，保持基础 CRUD 与核心流程可用。

## 六个工期任务划分
### 工期一（基建与需求固化）
- 明确角色与功能边界，输出需求清单与关键流程说明。
- 创建核心业务模型：SystemUser、DoctorDetail、PatientAttendant、Department/SubDepartment、Schedule、ScheduleTemplate、Registration、Room、ChatMessage、MedicalRecord、Prescription、Drug。
- 创建工程基础能力：统一返回体、异常处理、JWT 工具、鉴权拦截器、Redis 封装、基础路由与页面结构。
- 完成登录/注册/验证码/找回密码闭环，保证前后端可跑通。

### 工期二（挂号与排班）
- 科室/子科室查询与排班列表展示。
- 挂号创建与挂号列表/详情。
- 当前实现口径：挂号创建采用 `Redis 预占 + RabbitMQ 异步落库`，并以 `personKey` 保证“同一个真实人同一排班只能挂一次”。
- 当前超时口径：挂号预占仅保留 `zexp` 业务超时，已移除 `lease`；当前默认 `processing ttl = 20s`。
- 当前容灾口径：`DB` 是最终真相源；如果 `Redis` 崩溃且数据丢失，尚未在 `20s` 内持久化到 `DB` 的处理中挂号统一视为失败，用户重试；恢复后按 `DB` 重建库存，不恢复旧 `once/resv/zexp`。
- 挂号状态推进与补偿式对账。
- 排班模板管理与按日自动生成排班，提供“手动补偿生成”入口（幂等）。

### 工期三（问诊与聊天）
- Spring WebSocket 长连接与房间消息广播。
- 问诊发起/响应/超时处理。
- 聊天消息持久化与图片上传。

### 工期四（病历与处方）
- 病历录入与医生端查看。
- 处方创建与患者端查询。

### 工期五（管理端基础）
- 科室、医生、患者、药品管理的基础 CRUD。
- 系统用户状态与基础权限控制。
- 统计报表先保留页面壳与基础数据查询，聚合统计为可选项。

### 工期六（联调与验收）
- 端到端流程联调与缺陷修复。
- 性能与稳定性优化（连接/超时/重连）。
- 文档完善与最终验收材料整理。

## 当前变更同步（已完成）
- WebSocket 仅保留 Spring 原生 WebSocket 实现，旧 Netty 路径已下线。
- 前后端 WS 路径与诊断工具统一到 `/treat/ws/chat`。
- 挂号状态码对齐并补齐“等待患者确认/失效”。
- 移除无后端支撑的占位接口与空 Controller。
