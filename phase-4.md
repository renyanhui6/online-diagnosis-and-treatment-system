# 第四工期工作内容与图示（病历与处方阶段）

## 工作内容（详细）
1) 病历模型收口与字段口径统一  
- 第四工期的主目标，是把第三工期已经跑通的“挂号 → 问诊 → 聊天”结果真正沉淀为诊疗结果数据，也就是病历和处方。  
- 当前版本的 `medical_record` 实体保持轻量，核心字段集中在 `patientId`、`doctorId`、`doctorDescription`、`isPurchasable`、`createTime`。这里没有一开始就把病历拆成非常复杂的大字段集合，而是先围绕“能形成诊后结果闭环”做最小可用实现。  
- 其中 `doctorDescription` 作为本期病历录入的主要文本承载字段，承担了“病历摘要/医生结论/处置建议”的落地作用；`isPurchasable` 则统一承担处方状态字段：`2` 表示未开具处方，`0` 表示已开具未使用，`1` 表示已使用。  
- 实际联调时发现，如果病历和处方状态不在同一条主线上维护，患者端“病历有了但处方状态不对”、医生端“已经开药但列表仍显示未开具”的问题会非常明显，因此本期把病历主记录和处方状态强行收敛到一套口径里处理。  

2) 医生端病历录入与真实医生身份回填  
- 医生端新增病历接口后，不再信任前端传来的医生 ID，而是统一从登录态中取出 `userId`，再反查 `doctorId`，最终写入病历。这一步的目的很直接：避免前端状态漂移、缓存残留或页面切换后把病历挂到错误医生名下。  
- 新增病历时，系统会先把 `isPurchasable` 初始化为 `2(未开具)`，表示该次问诊已经形成病历，但尚未生成处方；病历创建成功后返回 `medicalRecordId`，供医生端后续继续开具处方。  
- 医生端病历管理页则按当前登录医生查询名下病历，并联表回填患者姓名、电话、年龄、性别、挂号 ID、房间 ID、医生描述和处方状态等展示字段。这样医生侧查看的不是单纯表记录，而是“围绕诊疗结果的完整视图”。  
- 做到这一步时，一个非常关键的修正是：病历归属必须以“后端反查真实医生”为准，而不是让前端带着 doctorId 直接提交。因为病历一旦写错医生，后续患者端回看、医生端病历列表和论文里的结果链路都会被污染。  

3) 处方创建与病历绑定  
- 第四工期把处方设计为“附属于病历”的结果数据，而不是独立脱离病历存在。医生提交处方时，前端先选择药品与数量，后端再按 `medicalRecordId` 批量写入 `prescription` 明细记录。  
- 当前 `prescription` 记录核心字段为 `drugId`、`drugQuantity`、`medicalRecordId`、`createTime`。也就是说，系统保存的是“病历下用了哪些药、每种药开了多少”，而药品名称、价格、单位、处方药属性等展示信息，在查询时通过药品库联表补齐。  
- 处方保存成功后，系统会反向把对应病历的 `isPurchasable` 从 `2` 更新为 `0`，表示该病历已开具处方但尚未完成后续使用流转。这样病历与处方就不是两套松散状态，而是前后关联的。  
- 这个阶段特意没有继续扩展“在线购药/支付/配送”链路，而是把范围收敛在“医生开具处方 + 患者查看处方结果”上。原因是本课题的主线是在线诊疗闭环，而不是药房电商系统；如果把处方再强行拉到购药链路，工作量会明显膨胀，而且会稀释核心业务。  

4) 患者端病历列表与处方明细查询  
- 患者端在第四工期接入就诊记录页，支持按当前登录用户查询病历列表，并查看每条病历对应的处方明细。  
- 病历列表展示的不是原始主表字段，而是经过联表加工后的 `MedicalRecordInfo`：包含患者姓名、联系电话、性别、年龄、医生姓名、挂号 ID、房间 ID、病历 ID、创建时间以及处方状态。  
- 当患者点击某条病历后，系统再根据 `medicalRecordId` 查询处方明细，展示药品名称、数量、单位、价格以及处方药属性，从而形成“诊后结果可追溯”的页面闭环。  
- 实际联调时最容易出现的问题不是查不到数据，而是展示口径不一致。例如病历列表中的医生姓名、聊天页面中的医生姓名、挂号列表中的医生姓名如果来自不同快照或不同表，就会让患者误以为不是同一次诊疗。因此本期重点之一就是把病历、处方、挂号的展示主数据源统一起来。  

5) 药品库支撑与医生端选药入口  
- 药品数据并不是处方模块临时写死的，而是复用管理端维护好的 `drug` 主数据。这样医生开具处方时选中的药品，和管理端药品库、患者端看到的药品明细天然一致。  
- 医生端提供药品列表与搜索接口，用于处方录入时选择药品；患者端则只读处方明细，不参与修改。这个边界有意保持清晰：药品主数据由后台治理，处方行为由医生执行，患者只负责查看结果。  
- 这一步做完以后，第四期就不是简单“新增两张表”，而是把药品主数据、病历结果和患者可视化查询串成了真正可运行的链路。  

6) 前后端联调与第四期实际收口  
- 医生端已经形成“聊天/问诊结束 → 填写病历 → 开具处方”的操作路径；患者端则形成“查看就诊记录 → 查看处方明细”的回看路径。  
- 联调过程中最典型的问题有两个。第一个是医生身份来源不稳定：早期如果直接用前端带来的 doctorId，病历极易挂错归属；后来统一改成后端按登录态推导。第二个是处方状态和病历状态不同步：早期只写处方表不回写病历状态，患者端列表会一直显示“未开具”。  
- 第四工期收口后，系统主链路已经从“问诊中”真正推进到了“诊疗结果形成并可回看”。这意味着论文里的核心业务闭环不再只停留在挂号和聊天，而是已经覆盖到病历与处方结果的最终落地。  

## 论文撰写建议

1) 本期在论文里建议突出“诊疗结果闭环”  
- 第三工期结束时，系统已经能完成挂号、接诊和聊天；第四工期的价值在于把聊天结果真正沉淀成病历和处方。  
- 写作时可以强调：没有第四期，系统只算“在线沟通工具”；补上第四期后，才真正形成“在线诊疗系统”的完整结果链路。  

2) 建议在正文中强调两个收敛动作  
- 医生身份不再由前端传入，而是由后端按登录态和医生表反查，保证病历归属正确。  
- 处方状态统一收敛到 `medical_record.isPurchasable` 上，避免病历与处方页面展示错位。  

3) 建议在测试章节补一句结果描述  
- 可以直接写成：`医生端已完成病历录入与处方开具，患者端已完成病历摘要和处方明细回看，说明诊疗结果链路已真实打通。`  

4) 如果后续要把本期内容并入论文正文  
- 可对应并入 `第5章 系统实现` 中的“病历与处方模块实现”小节；  
- 图示可择优放入“系统实现”或“系统设计”章节；  
- 若篇幅有限，本期最值得保留的是：病历归属修正、处方与病历状态联动、患者端结果回看三点。  
